# 确认阶段

本文件用于指导“生成前确认”。

## 目标

在真正出图前，至少把下面几件事确认清楚：

- 用哪套 `preset` 或 `style + layout`
- 需要几张卡片
- 是否需要参考图
- 是否需要水印
- 是否进入 `--yes` 非交互模式

## 推荐顺序

1. 先确认内容拆成几张
2. 再确认视觉方案
3. 最后确认是否直接执行

## 推荐输出

向用户给出：

- 推荐方案
- 备选方案
- 为什么推荐

如果用户没有补充意见，就按推荐方案走。

